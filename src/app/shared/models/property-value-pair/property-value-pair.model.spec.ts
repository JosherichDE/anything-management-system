import { PropertyValuePair } from './property-value-pair.model';

describe('PropertyValue', () => {
  it('should create an instance', () => {
    expect(new PropertyValuePair()).toBeTruthy();
  });
});
