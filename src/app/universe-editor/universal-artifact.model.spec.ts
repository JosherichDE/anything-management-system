import { UniversalArtifact } from './universal-artifact.model';

describe('UniversalArtifact', () => {
  it('should create an instance', () => {
    expect(new UniversalArtifact()).toBeTruthy();
  });
});
